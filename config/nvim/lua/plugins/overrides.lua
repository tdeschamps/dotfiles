-- Your own plugin specs and overrides go here.
-- LazyVim's defaults: https://www.lazyvim.org/configuration/plugins
--
-- Anything returned from this file is merged on top of LazyVim's spec, so you
-- can add plugins, change opts, or disable a LazyVim default with
-- `{ "author/plugin", enabled = false }`.
return {}
